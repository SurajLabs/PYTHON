a = (1, 2, 3, 4, 5)
print(type(a))

# Tuples are immutable, meaning that once they are created, their elements cannot be changed, added, or removed.
# Tuples are defined using parentheses () instead of square brackets [] like lists.
# It contains a sequence of elements, which can be of different data types, including numbers, strings, and other objects.

# Tuples are often used to group related data together, such as coordinates, RGB values, or database records.   

