s = {2, 4, 5, 8, 7, 7}

e = set() # Don't use s = {} as it will create an empty dictionary

print(s) # output: {2, 4, 5, 7, 8}

# set contains number, strings, etc. and it will not allow duplicate values. It will only store unique values.
# it sort the values in ascending order and it will not maintain the order of insertion.